import { NextPage } from "next";
import React from "react";
import Layout from "../../Layout";

const HtmlFormatterPage: NextPage = () => {
  return <Layout title="HTML Formatter">html formatter</Layout>;
};

export default HtmlFormatterPage;
