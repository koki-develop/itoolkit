import { NextPage } from "next";
import Layout from "../../Layout";

const UrlEncodeDecodePage: NextPage = () => {
  return <Layout>url encode/decode</Layout>;
};

export default UrlEncodeDecodePage;
